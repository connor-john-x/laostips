---
import { defaultLang, type Lang } from "../i18n/config";
import { formatDate } from "../i18n/utils";

interface Props {
	date: Date;
	lang?: Lang;
}

const { date, lang = defaultLang } = Astro.props;
---

<time datetime={date.toISOString()}>{formatDate(date, lang)}</time>
